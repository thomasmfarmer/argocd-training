apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitops-testing.fullname" . }}-test-connection"
  labels:
    {{- include "gitops-testing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitops-testing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
